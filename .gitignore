/build-native/
/build-emscripten/
/.ccls-cache/
/.vscode/
/.cache/
/.claude/
.gemini/
gha-creds-*.json

# Test programs assembled by the CPU test helper. ctest runs the tests from
# the build directory, but running a test binary by hand lands these in
# whatever directory it was started from.
/*.asm
/*.com

